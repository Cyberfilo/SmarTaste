services:
  - type: worker
    name: smartaste-enrichment-worker
    runtime: docker
    dockerfilePath: worker/Dockerfile
    dockerContext: .
    envVars:
      - key: DATABASE_URL
        sync: false  # Set manually to Railway PostgreSQL URL
      - key: WORKER_POLL_INTERVAL
        value: 120
