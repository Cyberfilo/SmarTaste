---
phase: 12-multi-llm-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/src/musicmind/api/chat/providers/__init__.py
  - backend/src/musicmind/api/chat/providers/base.py
  - backend/src/musicmind/api/chat/providers/claude.py
  - backend/src/musicmind/api/chat/providers/openai.py
  - backend/src/musicmind/api/chat/system_prompt.py
  - backend/src/musicmind/api/chat/tool_converter.py
  - backend/src/musicmind/api/chat/service.py
  - backend/src/musicmind/api/chat/schemas.py
  - backend/src/musicmind/api/chat/router.py
  - backend/src/musicmind/api/openai/__init__.py
  - backend/src/musicmind/api/openai/service.py
  - backend/src/musicmind/api/openai/router.py
  - backend/src/musicmind/api/openai/schemas.py
  - backend/src/musicmind/api/router.py
  - backend/tests/test_tool_converter.py
  - backend/tests/test_openai_byok.py
  - backend/tests/test_chat_providers.py
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - CHAT-07
  - CHAT-08
  - CHAT-09
  - CHAT-10

must_haves:
  truths:
    - "Chat works with Claude using the new provider abstraction (existing behavior preserved)"
    - "Chat works with OpenAI GPT-4o using the same tool registry"
    - "Both providers yield identical SSE event format (text, tool_start, tool_end, error, done)"
    - "User can store and validate an OpenAI API key"
    - "Tool definitions convert correctly between Anthropic and OpenAI formats"
    - "Both providers receive the same music-domain system prompt"
  artifacts:
    - path: "backend/src/musicmind/api/chat/providers/base.py"
      provides: "Abstract LLMProvider interface"
      contains: "class LLMProvider"
    - path: "backend/src/musicmind/api/chat/providers/claude.py"
      provides: "Claude provider implementation"
      contains: "class ClaudeProvider"
    - path: "backend/src/musicmind/api/chat/providers/openai.py"
      provides: "OpenAI provider implementation"
      contains: "class OpenAIProvider"
    - path: "backend/src/musicmind/api/chat/system_prompt.py"
      provides: "Shared system prompt builder"
      contains: "build_system_prompt"
    - path: "backend/src/musicmind/api/chat/tool_converter.py"
      provides: "Anthropic-to-OpenAI tool definition converter"
      contains: "to_openai_functions"
    - path: "backend/src/musicmind/api/openai/service.py"
      provides: "OpenAI BYOK key management"
      contains: "store_api_key"
    - path: "backend/src/musicmind/api/openai/router.py"
      provides: "OpenAI BYOK endpoints"
      contains: "router"
  key_links:
    - from: "backend/src/musicmind/api/chat/service.py"
      to: "backend/src/musicmind/api/chat/providers/base.py"
      via: "ChatService dispatches to LLMProvider based on model param"
      pattern: "provider\\.stream_response"
    - from: "backend/src/musicmind/api/chat/providers/openai.py"
      to: "backend/src/musicmind/api/chat/tool_converter.py"
      via: "OpenAIProvider converts tool defs before API call"
      pattern: "to_openai_functions"
    - from: "backend/src/musicmind/api/chat/router.py"
      to: "backend/src/musicmind/api/chat/service.py"
      via: "Router passes model param from request to ChatService"
      pattern: "model.*body\\.model"
---

<objective>
Backend LLM provider abstraction, OpenAI BYOK, and chat service refactor.

Purpose: Create the provider pattern (D-07 through D-10) that lets ChatService dispatch to either ClaudeProvider or OpenAIProvider, both yielding identical SSE events. Add OpenAI BYOK endpoints (D-01 through D-03) mirroring the Claude pattern. Extract the system prompt into a shared module (D-11, D-12). Build tool definition converter (D-13, D-14). Add openai>=1.60 dependency (D-15).

Output: Working backend that can chat via either Claude or OpenAI, with BYOK management for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-llm-support/12-CONTEXT.md

@backend/src/musicmind/api/chat/service.py
@backend/src/musicmind/api/chat/tools.py
@backend/src/musicmind/api/chat/router.py
@backend/src/musicmind/api/chat/schemas.py
@backend/src/musicmind/api/claude/service.py
@backend/src/musicmind/api/claude/router.py
@backend/src/musicmind/api/claude/schemas.py
@backend/src/musicmind/api/router.py
@backend/src/musicmind/db/schema.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/src/musicmind/db/schema.py (user_api_keys already supports multiple services):
```python
user_api_keys = sa.Table(
    "user_api_keys",
    metadata,
    sa.Column("user_id", sa.Text, sa.ForeignKey("users.id", ondelete="CASCADE"), nullable=False),
    sa.Column("service", sa.Text, nullable=False, server_default="anthropic"),
    sa.Column("api_key_encrypted", sa.Text, nullable=False),
    sa.Column("created_at", sa.DateTime(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.PrimaryKeyConstraint("user_id", "service"),
)
```

From backend/src/musicmind/api/chat/tools.py:
```python
TOOL_DEFINITIONS: list[dict[str, Any]]  # Anthropic format: name, description, input_schema
TOOL_EXECUTORS: dict[str, Any]          # tool_name -> async executor function
```

From backend/src/musicmind/api/claude/service.py (pattern to replicate for OpenAI):
```python
async def store_api_key(engine, encryption, *, user_id, api_key) -> None
async def get_api_key_status(engine, encryption, *, user_id) -> dict
async def get_decrypted_api_key(engine, encryption, *, user_id) -> str | None
async def delete_api_key(engine, *, user_id) -> bool
async def validate_anthropic_key(api_key: str) -> dict
def mask_api_key(api_key: str) -> str
def estimate_chat_cost() -> dict
```

From backend/src/musicmind/api/chat/service.py (SSE event format both providers must yield):
```python
# Events yielded by send_message:
{"event": "conversation_id", "data": {"id": conversation_id}}
{"event": "text", "data": {"text": delta_text}}
{"event": "tool_start", "data": {"tool": tool_name, "input": tool_input}}
{"event": "tool_end", "data": {"tool": tool_name, "result": result}}
{"event": "error", "data": {"message": error_message}}
{"event": "done", "data": {}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLM provider interface, tool converter, system prompt, and OpenAI BYOK</name>
  <files>
    backend/pyproject.toml
    backend/src/musicmind/api/chat/providers/__init__.py
    backend/src/musicmind/api/chat/providers/base.py
    backend/src/musicmind/api/chat/providers/claude.py
    backend/src/musicmind/api/chat/providers/openai.py
    backend/src/musicmind/api/chat/system_prompt.py
    backend/src/musicmind/api/chat/tool_converter.py
    backend/src/musicmind/api/openai/__init__.py
    backend/src/musicmind/api/openai/service.py
    backend/src/musicmind/api/openai/router.py
    backend/src/musicmind/api/openai/schemas.py
    backend/src/musicmind/api/router.py
  </files>
  <read_first>
    backend/src/musicmind/api/chat/service.py
    backend/src/musicmind/api/chat/tools.py
    backend/src/musicmind/api/claude/service.py
    backend/src/musicmind/api/claude/router.py
    backend/src/musicmind/api/claude/schemas.py
    backend/src/musicmind/api/router.py
    backend/pyproject.toml
    backend/src/musicmind/db/schema.py
  </read_first>
  <action>
    **1. Add openai dependency (per D-15):**
    In backend/pyproject.toml, add `"openai>=1.60"` to the dependencies list alongside the existing `"anthropic>=0.40"`.

    **2. Create providers/__init__.py:**
    Empty file (per convention).

    **3. Create providers/base.py (per D-07):**
    Define an abstract base class `LLMProvider` with:
    - `async def stream_response(self, *, api_key: str, messages: list[dict[str, Any]], system_prompt: str, tools: list[dict[str, Any]], tool_executors: dict[str, Any], engine, encryption, settings, user_id: str) -> AsyncGenerator[dict[str, Any], None]`
    - This method yields SSE event dicts in the format: `{"event": "text"|"tool_start"|"tool_end"|"error"|"done", "data": {...}}`
    - Use `from abc import ABC, abstractmethod`
    - Add constants: `MAX_TOOL_CALLS = 10`
    - Add `_execute_tool` as a concrete method (shared logic from current ChatService._execute_tool)

    **4. Create providers/claude.py (per D-08):**
    Class `ClaudeProvider(LLMProvider)` that wraps the existing agentic loop from ChatService.send_message (lines 105-267).
    - Uses `anthropic.AsyncAnthropic(api_key=api_key)`
    - Uses `client.messages.stream()` with `model="claude-sonnet-4-20250514"`
    - Streams text deltas, handles tool_use blocks, executes tools via `self._execute_tool()`
    - Catches `anthropic.AuthenticationError`, `RateLimitError`, `APIStatusError`, `APIError` and yields error events
    - `tool_choice={"type": "auto", "disable_parallel_tool_use": True}` (existing behavior)
    - Import TOOL_DEFINITIONS from tools.py for the Anthropic-format tools

    **5. Create providers/openai.py (per D-09):**
    Class `OpenAIProvider(LLMProvider)`:
    - Uses `openai.AsyncOpenAI(api_key=api_key)`
    - Uses `client.chat.completions.create(model="gpt-4o", stream=True, ...)`
    - Converts TOOL_DEFINITIONS to OpenAI function format via `to_openai_functions()` before the API call
    - OpenAI streaming: iterate over `async for chunk in stream:`, check `chunk.choices[0].delta`
    - For tool calls: accumulate `delta.tool_calls[0].function.arguments` across chunks, parse JSON on finish_reason="tool_calls"
    - Execute tools via `self._execute_tool()`, then send a follow-up message with role="tool" containing the result
    - Yield the same SSE events as ClaudeProvider: text, tool_start, tool_end, error, done
    - Catch `openai.AuthenticationError`, `openai.RateLimitError`, `openai.APIStatusError` with user-friendly error messages
    - System prompt goes as `{"role": "system", "content": system_prompt}` as the first message (OpenAI format)
    - Map finish_reason="stop" to end of response, "tool_calls" to tool execution loop

    **6. Create system_prompt.py (per D-11, D-12):**
    Extract `_build_system_prompt`, `_get_connected_services`, and `_get_taste_summary` from ChatService into a standalone module:
    - `async def build_system_prompt(engine, user_id: str) -> str`
    - `async def _get_connected_services(engine, user_id: str) -> list[str]`
    - `async def _get_taste_summary(engine, user_id: str) -> str | None`
    - Enhance the system prompt text per D-11 from CONTEXT.md:
      "You are MusicMind, an AI music discovery assistant. You have access to the user's real listening data from Spotify and Apple Music. You can analyze their taste profile, find new music they'll love, explain why songs match their preferences, and adjust recommendations based on natural language. Be specific -- reference actual tracks, artists, genres, and scoring dimensions. Be conversational, not clinical."
    - Keep the connected services and taste summary sections
    - Keep the available tools listing

    **7. Create tool_converter.py (per D-13, D-14):**
    - `def to_openai_functions(anthropic_tools: list[dict[str, Any]]) -> list[dict[str, Any]]`
    - Converts each Anthropic tool dict `{"name", "description", "input_schema"}` to OpenAI function dict `{"type": "function", "function": {"name", "description", "parameters"}}`
    - `input_schema` maps directly to `parameters` (both are JSON Schema)
    - TOOL_EXECUTORS remain unchanged -- only the definition format differs

    **8. Create OpenAI BYOK module (per D-01, D-02, D-03):**
    Duplicate the Claude BYOK pattern (`backend/src/musicmind/api/claude/`) as `backend/src/musicmind/api/openai/`:

    **openai/__init__.py:** Empty.

    **openai/schemas.py:** Same structure as claude/schemas.py:
    - `StoreKeyRequest`: api_key field (placeholder "sk-...")
    - `KeyStatusResponse`: configured, masked_key, service="openai"
    - `ValidateKeyResponse`: valid, error
    - `CostEstimateResponse`: model (gpt-4o), estimated_cost_per_message, input/output token prices

    **openai/service.py:** Mirror claude/service.py but with service="openai":
    - `mask_api_key(api_key)`: show "sk-...{last4}"
    - `estimate_chat_cost()`: GPT-4o pricing: input $2.50/1M, output $10.00/1M, est. per message $0.01-0.04
    - `store_api_key(engine, encryption, *, user_id, api_key)`: service="openai" in DB
    - `get_api_key_status(engine, encryption, *, user_id)`: service="openai"
    - `get_decrypted_api_key(engine, encryption, *, user_id)`: service="openai"
    - `delete_api_key(engine, *, user_id)`: service="openai"
    - `validate_openai_key(api_key)`: Create `openai.AsyncOpenAI(api_key=api_key)`, call `client.chat.completions.create(model="gpt-4o", max_tokens=1, messages=[{"role": "user", "content": "hi"}])`. Catch `openai.AuthenticationError` -> invalid. Per D-03.

    **openai/router.py:** Mirror claude/router.py with prefix="/api/openai":
    - POST /api/openai/key -> store key (per D-02)
    - GET /api/openai/key/status -> key status
    - POST /api/openai/key/validate -> validate key (per D-03)
    - DELETE /api/openai/key -> remove key
    - GET /api/openai/key/cost -> cost estimate

    **9. Wire OpenAI router in api/router.py:**
    Add `from musicmind.api.openai.router import router as openai_router` and `api_router.include_router(openai_router)`.

    **Important conventions to follow:**
    - `from __future__ import annotations` at top of every .py file
    - Type hints on all functions including `-> None`
    - Use `logger = logging.getLogger(__name__)` in each module
    - Use `X | None` not `Optional[X]`
    - Docstrings on all classes and public functions
    - Line length 100 max
  </action>
  <verify>
    <automated>cd /Users/filippomattiamenghi/musicmind/backend && uv run ruff check src/musicmind/api/chat/providers/ src/musicmind/api/openai/ src/musicmind/api/chat/system_prompt.py src/musicmind/api/chat/tool_converter.py src/musicmind/api/router.py && uv run python -c "from musicmind.api.chat.providers.base import LLMProvider; from musicmind.api.chat.providers.claude import ClaudeProvider; from musicmind.api.chat.providers.openai import OpenAIProvider; from musicmind.api.chat.system_prompt import build_system_prompt; from musicmind.api.chat.tool_converter import to_openai_functions; from musicmind.api.openai.service import validate_openai_key; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class LLMProvider" backend/src/musicmind/api/chat/providers/base.py
    - grep -q "class ClaudeProvider" backend/src/musicmind/api/chat/providers/claude.py
    - grep -q "class OpenAIProvider" backend/src/musicmind/api/chat/providers/openai.py
    - grep -q "build_system_prompt" backend/src/musicmind/api/chat/system_prompt.py
    - grep -q "to_openai_functions" backend/src/musicmind/api/chat/tool_converter.py
    - grep -q 'service="openai"' backend/src/musicmind/api/openai/service.py
    - grep -q "openai_router" backend/src/musicmind/api/router.py
    - grep -q '"openai>=1.60"' backend/pyproject.toml
  </acceptance_criteria>
  <done>
    LLMProvider ABC exists with ClaudeProvider and OpenAIProvider implementations. Both yield identical SSE event dicts. System prompt is shared module. Tool converter transforms Anthropic->OpenAI format. OpenAI BYOK endpoints exist at /api/openai/key/*. openai>=1.60 added to dependencies. All new code passes ruff check and imports cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor ChatService + router to dispatch by model, and add tests</name>
  <files>
    backend/src/musicmind/api/chat/service.py
    backend/src/musicmind/api/chat/schemas.py
    backend/src/musicmind/api/chat/router.py
    backend/tests/test_tool_converter.py
    backend/tests/test_openai_byok.py
    backend/tests/test_chat_providers.py
  </files>
  <read_first>
    backend/src/musicmind/api/chat/service.py
    backend/src/musicmind/api/chat/router.py
    backend/src/musicmind/api/chat/schemas.py
    backend/src/musicmind/api/chat/providers/base.py
    backend/src/musicmind/api/chat/providers/claude.py
    backend/src/musicmind/api/chat/providers/openai.py
    backend/src/musicmind/api/chat/system_prompt.py
    backend/src/musicmind/api/chat/tool_converter.py
    backend/src/musicmind/api/openai/service.py
    backend/src/musicmind/api/openai/router.py
    backend/tests/test_chat_endpoints.py
    backend/tests/test_chat_service.py
  </read_first>
  <action>
    **1. Update SendMessageRequest in schemas.py (per D-05):**
    Add `model: str | None = Field(default=None, description="LLM model override: 'claude' or 'openai'. If None, uses user's preferred model or defaults to 'claude'.")` to `SendMessageRequest`.

    **2. Refactor ChatService in service.py:**
    The existing ChatService.send_message has the Anthropic agentic loop inline. Refactor it:
    - Remove all Anthropic-specific code (the `while True` loop, `anthropic.*` imports, etc.)
    - Remove `_build_system_prompt`, `_get_connected_services`, `_get_taste_summary` (now in system_prompt.py)
    - Keep: `_create_conversation`, `_load_conversation_messages`, `_persist_messages`, `_serialize_message`, `_to_anthropic_messages` (rename to `_to_stored_messages`)
    - In `send_message()`:
      a. BYOK key retrieval: determine which provider to use from `model` param (default "claude"). If "openai", import and use `get_decrypted_api_key` from `musicmind.api.openai.service`; if "claude", use from `musicmind.api.claude.service`.
      b. If no key found, yield error event with appropriate message ("No Claude API key" or "No OpenAI API key").
      c. Create the appropriate provider: `ClaudeProvider()` or `OpenAIProvider()`.
      d. Build system prompt via `build_system_prompt(engine, user_id)`.
      e. Stream events from `provider.stream_response(...)`, yielding each event as-is.
      f. After streaming completes, persist messages as before.
    - The `send_message` signature gains `model: str | None = None` parameter.
    - Keep `MODEL = "claude-sonnet-4-20250514"` removed (model selection is in providers now).

    **3. Update chat router (per D-05):**
    In router.py `send_message` endpoint, pass `body.model` to `chat_service.send_message(... model=body.model)`.

    **4. Create test_tool_converter.py:**
    Test the Anthropic->OpenAI tool definition conversion:
    - Test that each of the 8 TOOL_DEFINITIONS converts to valid OpenAI function format
    - Test `name` and `description` are preserved
    - Test `input_schema` maps to `parameters`
    - Test empty input_schema produces valid output
    - Test required fields are carried over

    **5. Create test_openai_byok.py:**
    Integration tests mirroring test_claude_byok.py pattern (read that file first for the pattern):
    - Test POST /api/openai/key stores a key (201)
    - Test GET /api/openai/key/status returns configured=true after storing
    - Test GET /api/openai/key/status returns configured=false when no key
    - Test POST /api/openai/key/validate (mock validate_openai_key at router import level, same pattern as Claude tests)
    - Test DELETE /api/openai/key removes the key
    - Test DELETE /api/openai/key returns 404 when no key
    - Test GET /api/openai/key/cost returns pricing info
    - Follow existing test patterns: SQLite in-memory engine, CSRF token handling, hardcoded test user ID

    **6. Create test_chat_providers.py:**
    Unit tests for the provider dispatch:
    - Test that ChatService with model="claude" uses ClaudeProvider (mock the provider)
    - Test that ChatService with model="openai" uses OpenAIProvider (mock the provider)
    - Test that model=None defaults to "claude"
    - Test that missing API key for selected model yields the correct error event
    - Mock the providers at class level (same pattern as existing test_chat_service.py)

    **Ensure existing tests still pass:**
    The existing test_chat_endpoints.py and test_chat_service.py mock ChatService. Since we are changing its interface (adding model param), ensure backward compat: model param is optional and defaults to "claude", so existing tests should pass without changes. If they mock internal methods that were moved, update those mocks.
  </action>
  <verify>
    <automated>cd /Users/filippomattiamenghi/musicmind/backend && uv run ruff check src/musicmind/api/chat/ tests/test_tool_converter.py tests/test_openai_byok.py tests/test_chat_providers.py && uv run pytest tests/test_tool_converter.py tests/test_openai_byok.py tests/test_chat_providers.py tests/test_chat_endpoints.py tests/test_chat_service.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "model.*Field" backend/src/musicmind/api/chat/schemas.py
    - grep -q "ClaudeProvider\|OpenAIProvider" backend/src/musicmind/api/chat/service.py
    - grep -q "build_system_prompt" backend/src/musicmind/api/chat/service.py
    - grep -q "test_converts_tool_definitions" backend/tests/test_tool_converter.py
    - grep -q "test_store_openai_key" backend/tests/test_openai_byok.py
    - grep -q "test_.*provider" backend/tests/test_chat_providers.py
  </acceptance_criteria>
  <done>
    ChatService dispatches to ClaudeProvider or OpenAIProvider based on model param. SendMessageRequest has optional model field. Chat router passes model. All new tests pass. All existing chat tests still pass. Tool converter tested. OpenAI BYOK endpoints tested.
  </done>
</task>

</tasks>

<verification>
All backend tests pass:
```bash
cd backend && uv run pytest tests/ -x -v 2>&1 | tail -20
```

Lint clean:
```bash
cd backend && uv run ruff check src/ tests/
```

Import chain works:
```bash
cd backend && uv run python -c "
from musicmind.api.chat.service import ChatService
from musicmind.api.chat.providers.claude import ClaudeProvider
from musicmind.api.chat.providers.openai import OpenAIProvider
from musicmind.api.chat.tool_converter import to_openai_functions
from musicmind.api.chat.system_prompt import build_system_prompt
from musicmind.api.openai.router import router
print('All backend imports OK')
"
```
</verification>

<success_criteria>
- LLMProvider ABC with ClaudeProvider and OpenAIProvider that yield identical SSE events
- ChatService dispatches to correct provider based on model param
- OpenAI BYOK endpoints at /api/openai/key/* (store, status, validate, delete, cost)
- Tool definitions convert between Anthropic and OpenAI formats
- Shared system prompt module used by both providers
- openai>=1.60 in pyproject.toml
- All new tests pass, all existing tests pass, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-multi-llm-support/12-01-SUMMARY.md`
</output>
