---
phase: 12-multi-llm-support
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/hooks/use-openai-key.ts
  - frontend/src/components/settings/openai-key-manager.tsx
  - frontend/src/components/settings/model-selector.tsx
  - frontend/src/app/(app)/settings/page.tsx
  - frontend/src/hooks/use-chat.ts
  - frontend/src/lib/sse.ts
  - frontend/src/components/chat/chat-interface.tsx
  - frontend/src/components/chat/chat-input.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-05
  - CHAT-10

must_haves:
  truths:
    - "User can store and manage an OpenAI API key in settings"
    - "User can select preferred AI model (Claude or OpenAI) in settings"
    - "User can override the model per-conversation from the chat interface"
    - "Chat SSE stream passes model param to backend"
    - "Switching models preserves conversation history in UI"
  artifacts:
    - path: "frontend/src/hooks/use-openai-key.ts"
      provides: "OpenAI BYOK TanStack Query hooks"
      contains: "useOpenAIKeyStatus"
    - path: "frontend/src/components/settings/openai-key-manager.tsx"
      provides: "OpenAI key management card"
      contains: "OpenAIKeyManager"
    - path: "frontend/src/components/settings/model-selector.tsx"
      provides: "Model preference selector"
      contains: "ModelSelector"
    - path: "frontend/src/hooks/use-chat.ts"
      provides: "Chat store with model selection"
      contains: "selectedModel"
  key_links:
    - from: "frontend/src/lib/sse.ts"
      to: "/api/chat/message"
      via: "POST body includes model field"
      pattern: "model"
    - from: "frontend/src/hooks/use-chat.ts"
      to: "frontend/src/lib/sse.ts"
      via: "sendMessage passes selectedModel to streamChat"
      pattern: "selectedModel"
    - from: "frontend/src/components/chat/chat-interface.tsx"
      to: "frontend/src/hooks/use-chat.ts"
      via: "ChatInterface reads/sets selectedModel from store"
      pattern: "selectedModel"
---

<objective>
Frontend model selector, OpenAI BYOK key management, and chat model override.

Purpose: Give users the ability to manage an OpenAI API key (per D-02), select their preferred AI model (per D-04, D-06), and override the model per-conversation in the chat UI (per D-05). The frontend sends the model param in the SSE stream request so the backend dispatches to the correct provider.

Output: Settings page with OpenAI key manager and model selector. Chat interface with model override. SSE client passes model param.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-llm-support/12-CONTEXT.md
@.planning/phases/12-multi-llm-support/12-01-SUMMARY.md

@frontend/src/hooks/use-claude-key.ts
@frontend/src/components/settings/byok-key-manager.tsx
@frontend/src/app/(app)/settings/page.tsx
@frontend/src/hooks/use-chat.ts
@frontend/src/lib/sse.ts
@frontend/src/components/chat/chat-interface.tsx
@frontend/src/components/chat/chat-input.tsx

<interfaces>
<!-- Existing frontend patterns to follow -->

From frontend/src/hooks/use-claude-key.ts (pattern to duplicate for OpenAI):
```typescript
export function useKeyStatus(): UseQueryResult<KeyStatusResponse>
export function useStoreKey(): UseMutationResult<void, Error, string>
export function useValidateKey(): UseMutationResult<ValidateKeyResponse, Error>
export function useDeleteKey(): UseMutationResult<void, Error>
export function useCostEstimate(): UseQueryResult<CostEstimateResponse>
```

From frontend/src/lib/sse.ts (StreamChatParams to extend):
```typescript
export interface StreamChatParams {
  conversationId?: string;
  message: string;
}
// POST body sent to /api/chat/message
```

From frontend/src/hooks/use-chat.ts (Zustand store to extend):
```typescript
interface ChatState {
  messages: ChatMessage[];
  conversationId: string | null;
  isStreaming: boolean;
  activeTools: ActiveTool[];
  error: string | null;
  sendMessage: (text: string) => void;
  loadConversation: (id: string) => Promise<void>;
  newConversation: () => void;
  cancelStream: () => void;
  dismissError: () => void;
}
```

From frontend/src/lib/api.ts:
```typescript
export const API_URL: string;
export function apiFetch<T>(path: string, init?: RequestInit): Promise<T>;
```

Backend endpoints from Plan 01 (OpenAI BYOK):
- POST /api/openai/key — store key
- GET /api/openai/key/status — {configured, masked_key, service}
- POST /api/openai/key/validate — {valid, error}
- DELETE /api/openai/key — remove key
- GET /api/openai/key/cost — pricing info

Backend chat endpoint (updated in Plan 01):
- POST /api/chat/message — now accepts optional "model" field in body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAI key hooks, key manager component, and model selector</name>
  <files>
    frontend/src/hooks/use-openai-key.ts
    frontend/src/components/settings/openai-key-manager.tsx
    frontend/src/components/settings/model-selector.tsx
    frontend/src/app/(app)/settings/page.tsx
  </files>
  <read_first>
    frontend/src/hooks/use-claude-key.ts
    frontend/src/components/settings/byok-key-manager.tsx
    frontend/src/app/(app)/settings/page.tsx
    frontend/src/lib/api.ts
  </read_first>
  <action>
    **1. Create use-openai-key.ts:**
    Duplicate use-claude-key.ts with these changes:
    - All API paths change from `/api/claude/key*` to `/api/openai/key*`
    - Query keys change from `["claude-key-status"]` to `["openai-key-status"]`, `["claude-cost-estimate"]` to `["openai-cost-estimate"]`
    - Exports: `useOpenAIKeyStatus`, `useStoreOpenAIKey`, `useValidateOpenAIKey`, `useDeleteOpenAIKey`, `useOpenAICostEstimate`
    - Types same as Claude (KeyStatusResponse, ValidateKeyResponse, CostEstimateResponse)

    **2. Create openai-key-manager.tsx (per D-02):**
    Duplicate byok-key-manager.tsx with these changes:
    - Component name: `OpenAIKeyManager`
    - Title: "AI Assistant (OpenAI)"
    - Description: "Bring your own OpenAI API key to use ChatGPT models"
    - Import hooks from `use-openai-key` instead of `use-claude-key`
    - Placeholder text: "sk-..." (not "sk-ant-...")
    - "Get your API key" link points to `https://platform.openai.com/api-keys`
    - Help text: "Create a key -> copy it -> paste above -> Save Key. You pay OpenAI directly per usage."
    - Cost section shows "You pay OpenAI directly. MusicMind does not charge anything."
    - Icon: same Key icon from lucide-react

    **3. Create model-selector.tsx (per D-04, D-06):**
    A Card component for selecting the preferred AI model:
    - Title: "Preferred AI Model" with a Sparkles icon
    - Description: "Choose which AI model to use for chat (can override per-conversation)"
    - Radio group or segmented control with 3 options:
      - Claude Sonnet 4 (default) -- "Anthropic's latest, strong at reasoning"
      - GPT-4o -- "OpenAI's flagship, fast and capable"
      - GPT-4.1 -- "OpenAI's newest, improved coding and instruction following"
    - Selection stored in localStorage key "musicmind-preferred-model" with values: "claude", "gpt-4o", "gpt-4.1"
    - On change, update localStorage and show toast: "Preferred model updated to {name}"
    - Show a subtle note if the selected model's API key is not configured: "You need an OpenAI API key to use this model" (query both useKeyStatus and useOpenAIKeyStatus)
    - Use shadcn RadioGroup component (from @/components/ui/radio-group)
    - Import `useKeyStatus` from use-claude-key and `useOpenAIKeyStatus` from use-openai-key to check key config status

    **4. Update settings/page.tsx:**
    - Import `OpenAIKeyManager` from `@/components/settings/openai-key-manager`
    - Import `ModelSelector` from `@/components/settings/model-selector`
    - Add `<ModelSelector />` after `<BYOKKeyManager />`
    - Add `<OpenAIKeyManager />` after `<ModelSelector />`
    - Order: ServiceConnections -> BYOKKeyManager (Claude) -> ModelSelector -> OpenAIKeyManager
  </action>
  <verify>
    <automated>cd /Users/filippomattiamenghi/musicmind/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useOpenAIKeyStatus" frontend/src/hooks/use-openai-key.ts
    - grep -q "OpenAIKeyManager" frontend/src/components/settings/openai-key-manager.tsx
    - grep -q "ModelSelector" frontend/src/components/settings/model-selector.tsx
    - grep -q "OpenAIKeyManager" frontend/src/app/\(app\)/settings/page.tsx
    - grep -q "ModelSelector" frontend/src/app/\(app\)/settings/page.tsx
    - grep -q "musicmind-preferred-model" frontend/src/components/settings/model-selector.tsx
  </acceptance_criteria>
  <done>
    Settings page shows Claude key manager, model selector (Claude/GPT-4o/GPT-4.1), and OpenAI key manager. Model preference persists in localStorage. Key warnings show when selected model lacks a configured key.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire model selection into chat SSE stream and chat interface</name>
  <files>
    frontend/src/hooks/use-chat.ts
    frontend/src/lib/sse.ts
    frontend/src/components/chat/chat-interface.tsx
    frontend/src/components/chat/chat-input.tsx
  </files>
  <read_first>
    frontend/src/hooks/use-chat.ts
    frontend/src/lib/sse.ts
    frontend/src/components/chat/chat-interface.tsx
    frontend/src/components/chat/chat-input.tsx
    frontend/src/components/settings/model-selector.tsx
  </read_first>
  <action>
    **1. Update StreamChatParams in sse.ts:**
    - Add `model?: string` to `StreamChatParams`
    - In `streamChat()`, if `params.model` is truthy, include `model: params.model` in the POST body JSON

    **2. Update ChatState in use-chat.ts (per D-05):**
    Add to the Zustand store:
    - `selectedModel: string` -- initialized from `localStorage.getItem("musicmind-preferred-model") || "claude"`
    - `setSelectedModel: (model: string) => void` -- sets selectedModel in state AND localStorage
    - In `sendMessage`, pass `model: get().selectedModel` to `streamChat()`
    - Map model values for backend: "claude" stays "claude", "gpt-4o" and "gpt-4.1" map to "openai" (the backend provider name)
    - Actually, let the backend handle model IDs. Pass the selectedModel value directly: "claude" | "gpt-4o" | "gpt-4.1". The backend ChatService will use "claude" for "claude" and "openai" for "gpt-4o"/"gpt-4.1". Update: send a simple mapping: if selectedModel starts with "gpt", send model="openai" to the backend. The actual model ID (gpt-4o vs gpt-4.1) should also be sent. So extend the body to include both `model: "claude"|"openai"` (provider) and `model_id: "gpt-4o"|"gpt-4.1"|"claude-sonnet-4-20250514"` (specific model).
    - Simpler approach (per D-05): send `model` as the provider name. "claude" = Claude, "openai" = OpenAI. The specific model ID is managed by the backend providers. Map: "claude" -> "claude", "gpt-4o" -> "openai", "gpt-4.1" -> "openai". For now, the backend uses a single model per provider.

    **3. Update ChatInterface (per D-05, switching models preserves history):**
    - Add a model selector dropdown in the chat header area (between sidebar and message list)
    - Import `useChatStore` selector for `selectedModel` and `setSelectedModel`
    - Use a simple select/dropdown with 3 options: Claude Sonnet 4, GPT-4o, GPT-4.1
    - Styled as a small pill/badge dropdown in the top bar (not a full card -- this is the per-conversation override)
    - When user changes model in chat, it updates `selectedModel` in the store (and localStorage)
    - Messages remain in place (switching model does NOT clear conversation -- per phase success criteria #5)
    - Update the EmptyState heading: "Ask about your music" (generic, not "Ask Claude")
    - Update NoKeyState to check which model is selected and show the appropriate message:
      - If "claude" selected and no Claude key: "Claude API key required"
      - If "gpt-4o"/"gpt-4.1" selected and no OpenAI key: "OpenAI API key required"
    - Import `useKeyStatus` and `useOpenAIKeyStatus` to determine which key is missing

    **4. Update ChatInput:**
    - No functional changes needed -- ChatInput calls `onSend(text)` which goes to `sendMessage` in the store, which already includes the model
    - Optionally: show a subtle model indicator label below the input (e.g., "Powered by Claude" or "Powered by GPT-4o") using the selectedModel from the store

    **5. Update error mapping in use-chat.ts:**
    - Add "openai_key_missing" code mapping: "Your OpenAI API key is missing. Add it in Settings."
    - Existing error codes (key_expired, rate_limited, insufficient_balance) work for both providers
  </action>
  <verify>
    <automated>cd /Users/filippomattiamenghi/musicmind/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "selectedModel" frontend/src/hooks/use-chat.ts
    - grep -q "setSelectedModel" frontend/src/hooks/use-chat.ts
    - grep -q "model" frontend/src/lib/sse.ts
    - grep -q "selectedModel\|setSelectedModel" frontend/src/components/chat/chat-interface.tsx
  </acceptance_criteria>
  <done>
    Chat SSE client sends model param in POST body. Zustand store tracks selectedModel (initialized from localStorage). Chat interface has per-conversation model selector in header. Switching models preserves messages. NoKeyState adapts to selected model. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
TypeScript compiles:
```bash
cd frontend && npx tsc --noEmit
```

Settings page renders all three new components:
```bash
grep -c "OpenAIKeyManager\|ModelSelector\|BYOKKeyManager" frontend/src/app/\(app\)/settings/page.tsx
# Expected: 6 (3 imports + 3 usages)
```

Model param flows through SSE:
```bash
grep "model" frontend/src/lib/sse.ts | head -5
```
</verification>

<success_criteria>
- OpenAI key management UI in settings (store, validate, delete, cost)
- Model selector in settings persists preference to localStorage
- Per-conversation model override in chat header
- SSE client sends model param to backend
- Switching models preserves conversation messages
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-multi-llm-support/12-02-SUMMARY.md`
</output>
